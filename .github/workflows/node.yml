name: build

on:
  push:

jobs:
  build:
    # Provides the postgres service the integration tests run against and exposes it through
    # PG_COMPONENT_PSQL_CONNECTION_STRING, which takes precedence over the one in .env.test.
    uses: decentraland/platform-actions/.github/workflows/apps-with-db-build.yml@main
    with:
      node-version: '24.x'
